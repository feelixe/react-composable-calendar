import { markdownComponents } from "@/components/markdown/components";
import { CodeBlock } from "@/components/ui/code-block.tsx"
import { basicCalendar } from "@/examples/code-examples.ts"

export const components = markdownComponents;

# Installation
Install dependencies
<CodeBlock client:load language="bash" code="npm i react-composable-calendar dayjs" />

Copy paste the **examples** you want into your project and start building your perfect calendar!
