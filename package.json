{
  "name": "react-composable-calendar-workspace",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/bun": "latest",
    "typescript": "^6.0.2"
  },
  "private": true,
  "scripts": {
    "build:packages": "bun run --filter './packages/*' build"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
